var rPS= ["Rock", "Paper", "Scissors"] 
var results = {
    Wins: 0,
    Losses: 0,
    Ties: 0
}

var playerChoice = prompt("Please choose R,P,S")

function playGame(playerChoice) {
    var cpuChoice = Math.floor(Math.random() * rPS.length)

    if (playerChoice){}
}

